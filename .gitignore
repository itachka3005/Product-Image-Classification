# venv
venv/
.venv/
env/

# python
__pycache__/
*.pyc
*.pyo

# gradio
.gradio/

# датасеты — слишком большие для обычного git
dataset/
dataset_prepared/

# модель ViT (папка с весами, ~300+ МБ) — не коммитим по умолчанию
# если хочешь закоммитить, раскомментируй и настрой Git LFS
models/vit-product-classifier/

# старый формат модели (ResNet18), если остался
models/*.pth

# checkpoints от transformers.Trainer (промежуточные, не нужны)
checkpoints/

# логи / debug-дампы
logs/
output/debug_*
output/*.png
output/*.html

# скачанные архивы модели
*.zip

# ide
.vscode/
.idea/

# os
.DS_Store
Thumbs.db